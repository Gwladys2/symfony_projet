{% extends 'pins/index.html.twig' %}


{% block contenu %}

    <h1>Mettre à jour le Pin numero {{ pin.id }}</h1>
    <div style="width: 800px; padding: 20px">
        {{ form_start(form) }}

        {{ form_widget(form) }}

        <input type="submit" value="mettre à jour le Pin">
        {{ form_end(form) }}
    </div>
{% endblock %}