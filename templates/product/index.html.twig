{% extends 'contact_me/index.html.twig' %}

{% block title %}Nos Produits- La boutique de Gwladys{% endblock %}

{% block contenu %}
    <div class="row">

        <div class="col-md-9">
            <h3>Nos Produits</h3>

            <div class="row">
                {% for product in products %}
                <div class="col-md-4">
                    <div class="product-item text-center" style="margin-top: 50px">
                        <a href="{{ path('app_product', {'slug':product.slug}) }}" style="text-decoration: none; color: black" ><img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid">
                            <h5 style="text-transform: uppercase; font-size: 15px; margin-bottom: 1px">{{ product.name }}</h5>
                            <span class ="product-subtitle" style="display: block; margin-bottom: 10px">{{ product.subtitle }}</span>
                            <span class="product-price" style="font-weight: bold">{{ (product.price/100)|number_format(2) }} €</span></a>

                    </div>
                </div>


    {% endfor %}
    </div>
    </div>
    </div>
{% endblock %}
