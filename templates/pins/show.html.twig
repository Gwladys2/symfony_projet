{% extends 'pins/index.html.twig' %}


{% block contenu %}

<div style="background-color: black; height: 100px; padding-top: 30px; padding-left: 1200px; border-radius: 5px">
<a href="{{path('app_pins')  }}" style="color: white">Retour</a>
</div>

    <article>
            <h1>{{ pin.title }}</h1>
            <p>{{ pin.description }}</p>
        </article>

    <a href="{{ path('app_pins_edit', {id:pin.id}) }}">Modifier</a>
    <a href="{{ path('app_pins_delete', {id:pin.id}) }}" >Supprimer</a>
    <a href="{{ path('app_pins') }}">Retour</a>

{% endblock %}




